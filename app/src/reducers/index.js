const initialState = {
    isFetching: false, 
    stationsData: [], 
    error: ''
}; 

export const stationsReducer = (state = initialState, action) => {
    switch(action.type){
        default: 
        return state; 
    }
}